/** 
 Low-level operations on components, that have no high-level knowledge of the context.
 Each context might prefer to work with higher-level objects, and only "drop down" to
 the level of raw components when needed.
*/
package sr.core.component.ops;